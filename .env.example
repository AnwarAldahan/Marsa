# Copy this file to .env and fill in your own values. Never commit .env.
# Optional: enables Gemini narrative for the agents. Without it Marsa runs fully deterministic.
GEMINI_API_KEY=
# Optional: override the Gemini model name (leave empty for the default).
MARSA_GEMINI_MODEL=